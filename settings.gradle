pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url 'https://jitpack.io' }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        gradlePluginPortal()
        maven { url 'https://jitpack.io' }
        mavenCentral()
    }
}

include ':lib', ':app', ':launcher'
rootProject.name="VirtualApp202410"
